language: generic

sudo: required

services: 
  - docker

before_install:
  - docker build -t flower01/react-test-app -f ./frontend/Dockerfile.dev ./frontend

script:
  - docker run -e CI=true flower01/react-test-app npm run test

after_success:
  - docker build -t flower01/docker-frontend ./frontend
  - docker build -t flower01/docker-backend ./backend
  - docker build -t flower01/docker-nginx ./nginx

  - echo "$DOCKER_HUB_PASSWORD" | docker login -u "$DOCKER_HUB_ID" --password-stdin

  - docker push flower01/docker-frontend
  - docker push flower01/docker-backend
  - docker push flower01/docker-nginx